version: '3.8'

services:
  web:
    image: nginx:stable-alpine
    container_name: nginx-web
    restart: unless-stopped
    # 権限問題を避けるため一時的にrootで実行
    user: "0:0"
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./html:/usr/share/nginx/html:ro
    networks:
      - frontend-nw