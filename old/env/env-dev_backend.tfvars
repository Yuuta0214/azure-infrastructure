# ==========================================
# 1. 基盤・環境定義
# ==========================================
location     = "japanwest"
environment  = "dev"
project_name = "web"

# ==========================================
# 2. メタデータ定義 (運用管理用)
# ==========================================
# main.tf で自動付与される項目（Environment等）を除いた「固有の値」だけを書く
tags = {
  BusinessUnit = "Production-Dev"
  CostCenter   = "WEB-201"
}