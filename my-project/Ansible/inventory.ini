[docker_hosts]
# ansible_host: Terraformのoutputで出力されたパブリックIPをここに記述
# ansible_user: 機密情報ではないため、直接「azureuser」と記述します
vm_host ansible_host=xxx.xxx.xxx.xxx ansible_user=azureuser

[docker_hosts:vars]
# SSH接続時のインタープリタを指定（Debian 12に合わせて明示）
ansible_python_interpreter=/usr/bin/python3