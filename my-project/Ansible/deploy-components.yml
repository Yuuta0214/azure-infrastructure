- name: "Component-based Deployment of Web Service"
  hosts: all
  gather_facts: no

  vars:
    # Terraformから渡される admin_username を使用
    admin_user: "{{ admin_username }}"
    project_root: "/home/{{ admin_username }}/app"
    # Docker公式リポジトリ設定用
    docker_gpg_path: "/etc/apt/keyrings/docker.gpg"
    docker_repo_url: "https://download.docker.com/linux/debian"

    # --- 外部の実行引数(--extra-vars)から安全に値を受け取るための変数定義（完全復元） ---
    mysql_root_password: "{{ mysql_root_password }}"
    mysql_db_name: "{{ mysql_db_name }}"
    mysql_user: "{{ mysql_user }}"
    mysql_password: "{{ mysql_password }}"

  tasks:
    # ------------------------------------------
    # 0. OS基本設定フェーズ (NTP, Locale, Update)
    # ------------------------------------------
    - name: "Update all packages to latest version"
      ansible.builtin.apt:
        upgrade: dist
        update_cache: yes
      become: yes

    - name: "Install NTP/Timezone tools"
      ansible.builtin.package:
        name:
          - chrony
          - locales
        state: present
      become: yes

    - name: "Set timezone to Asia/Tokyo"
      community.general.timezone:
        name: Asia/Tokyo
      become: yes

    - name: "Ensure Japanese locale is generated"
      community.general.locale_gen:
        name: ja_JP.UTF-8
        state: present
      become: yes

    - name: "Set system default locale"
      ansible.builtin.command: localectl set-locale LANG=ja_JP.UTF-8
      become: yes
      changed_when: false

    # ------------------------------------------
    # 1. 同期・準備フェーズ (既存 site.yml から完全継承)
    # ------------------------------------------
    - name: "Wait for infrastructure bootstrap to complete"
      ansible.builtin.wait_for:
        path: /var/tmp/bootstrap_complete
        state: present
        timeout: 600
      become: no  # bootstrap.shの完了を待つだけなのでsudo不要

    - name: "Gather facts after bootstrap confirmation"
      ansible.builtin.setup:
      become: yes

    # ------------------------------------------
    # 2. Docker インストールフェーズ (既存 site.yml から完全継承)
    # ------------------------------------------
    - name: "Create directory for Docker GPG key"
      ansible.builtin.file:
        path: /etc/apt/keyrings
        state: directory
        mode: '0755'
      become: yes

    - name: "Add Docker's official GPG key"
      ansible.builtin.get_url:
        url: "https://download.docker.com/linux/debian/gpg"
        dest: "/etc/apt/keyrings/docker.asc"
        mode: '0644'
      become: yes

    - name: "Add Docker repository to Apt sources"
      ansible.builtin.apt_repository:
        repo: >
          deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.asc]
          https://download.docker.com/linux/debian
          {{ ansible_distribution_release }}
          stable
        state: present
        filename: docker
      become: yes

    - name: "Install Docker Engine and Compose plugin"
      ansible.builtin.apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-buildx-plugin
          - docker-compose-plugin
        state: present
        update_cache: yes
      become: yes

    - name: "Ensure Docker service is started and enabled"
      ansible.builtin.systemd:
        name: docker
        state: started
        enabled: yes
      become: yes

    # ------------------------------------------
    # 3. ポート競合自動回避フェーズ
    # ------------------------------------------
    - name: "Stop and remove all existing containers to free up port 80"
      ansible.builtin.shell: |
        # コンテナIDを取得して停止・削除
        containers=$(docker ps -q)
        if [ -n "$containers" ]; then
          docker rm -f $containers
        fi
      become: yes
      changed_when: false # 常に実行して問題ないため

    # ------------------------------------------
    # 4. ディレクトリ構造の配置・作成フェーズ (画像および新構成のファクト)
    # ------------------------------------------
    - name: "Create component directories on remote VM"
      ansible.builtin.file:
        path: "{{ project_root }}/components/{{ item }}"
        state: directory
        owner: "{{ admin_user }}"
        group: "{{ admin_user }}"
        mode: '0755'
      become: yes
      loop:
        - db
        - app
        - web

    - name: "Copy nginx.conf to project root"
      ansible.builtin.copy:
        src: "./nginx.conf"
        dest: "{{ project_root }}/components/web/nginx.conf"  # 👈 コピー先を components/web/ 配下に変更
        owner: "{{ admin_user }}"
        group: "{{ admin_user }}"
        mode: '0644'
      become: yes

    - name: "Copy html directory to project root"
      ansible.builtin.copy:
        src: "./html"
        dest: "{{ project_root }}/components/web/"            # 👈 コピー先を components/web/ 配下に変更（末尾の「git」は完全削除）
        owner: "{{ admin_user }}"
        group: "{{ admin_user }}"
        mode: '0755'
      become: yes

    # ------------------------------------------
    # 【確実な解消対応】共通ネットワークの事前確保フェーズ
    # ------------------------------------------
    - name: "Ensure shared docker networks exist"
      ansible.builtin.command:
        cmd: "docker network create {{ item }}"
      loop:
        - frontend-nw
        - backend-nw
      become: yes
      register: network_create
      failed_when: 
        - network_create.rc != 0
        - "'already exists' not in network_create.stderr"
      changed_when: "network_create.rc == 0"

    # ------------------------------------------
    # 5. DBコンテナ配置・起動フェーズ (最優先)
    # ------------------------------------------
    - name: "Deploy docker-compose-db.yml from template"
      ansible.builtin.template:
        src: "./components/db/docker-compose-db.yml.j2"
        dest: "{{ project_root }}/components/db/docker-compose.yml"
        owner: "{{ admin_user }}"
        group: "{{ admin_user }}"
        mode: '0644'
      become: yes

    - name: "Remove existing DB container"
      ansible.builtin.shell: "docker rm -f mysql-db || true"
      become: yes

    - name: "Run docker compose up for DB"
      ansible.builtin.command:
        cmd: "docker compose -p myapp up -d"  # ★プロジェクト名を指定
        chdir: "{{ project_root }}/components/db"
      become: yes

    # ------------------------------------------
    # 6. DBヘルスチェック監視フェーズ
    # ------------------------------------------
    - name: "Wait for DB container to become healthy"
      ansible.builtin.command:
        cmd: "{% raw %}docker inspect --format='({{.State.Health.Status}})' mysql-db{% endraw %}"
      register: db_health
      until: db_health.stdout == "(healthy)"
      retries: 12
      delay: 5
      become: yes
      changed_when: false

    # ------------------------------------------
    # 7. App・Webコンテナ配置・起動フェーズ
    # ------------------------------------------
    - name: "Create app source directory on remote VM"
      ansible.builtin.file:
        path: "{{ project_root }}/components/app/app"
        state: directory
        owner: "{{ admin_user }}"
        group: "{{ admin_user }}"
        mode: '0755'
      become: yes

    - name: "Deploy docker-compose-app.yml from template"
      ansible.builtin.template:
        src: "./components/app/docker-compose-app.yml.j2"
        dest: "{{ project_root }}/components/app/docker-compose.yml"
        owner: "{{ admin_user }}"
        group: "{{ admin_user }}"
        mode: '0644'
      become: yes

    - name: "Deploy docker-compose-web.yml from template"
      ansible.builtin.template:
        src: "./components/web/docker-compose-web.yml.j2"
        dest: "{{ project_root }}/components/web/docker-compose.yml"
        owner: "{{ admin_user }}"
        group: "{{ admin_user }}"
        mode: '0644'
      become: yes

    - name: "Remove existing App container"
      ansible.builtin.shell: "docker rm -f python-app || true"
      become: yes

    - name: "Run docker compose up for App"
      ansible.builtin.command:
        cmd: "docker compose -p myapp up -d"
        chdir: "{{ project_root }}/components/app"
      become: yes

    - name: "Remove existing Web container"
      ansible.builtin.shell: "docker rm -f nginx-web || true"
      become: yes

    - name: "Run docker compose up for Web"
      ansible.builtin.command:
        cmd: "docker compose -p myapp up -d"
        chdir: "{{ project_root }}/components/web"
      become: yes

    # ------------------------------------------
    # 8. ホスト Nginx インストール・設定フェーズ (★最後に実行)
    # ------------------------------------------
    - name: "Install Nginx on Host"
      ansible.builtin.apt:
        name: nginx
        state: present
        update_cache: yes
      become: yes

    - name: "Configure Nginx as Reverse Proxy"
      ansible.builtin.template:
        src: "./nginx-host.conf.j2"
        dest: "/etc/nginx/sites-available/default"
        mode: '0644'
      become: yes
      notify: Restart Nginx

    - name: "Ensure Nginx is started and enabled"
      ansible.builtin.systemd:
        name: nginx
        state: started
        enabled: yes
      become: yes

    # ------------------------------------------
    # 9. ホスト Nginx + ModSecurity インストール・設定フェーズ
    # ------------------------------------------
    - name: "Install Nginx and ModSecurity"
      ansible.builtin.apt:
        name:
          - nginx
          - libmodsecurity3
          - libnginx-mod-http-modsecurity
        state: present
        update_cache: yes
      become: yes

    - name: "Create ModSecurity configuration directory"
      ansible.builtin.file:
        path: /etc/nginx/modsec
        state: directory
        mode: '0755'
      become: yes

    - name: "Copy ModSecurity recommended configuration"
      ansible.builtin.copy:
        src: /usr/share/modsecurity-crs/modsecurity.conf-recommended # インストール済みパスからコピー
        dest: /etc/nginx/modsec/modsecurity.conf
        remote_src: yes
      become: yes

    - name: "Enable ModSecurity rule in main config"
      ansible.builtin.lineinfile:
        path: /etc/nginx/modsec/modsecurity.conf
        regexp: '^SecRuleEngine DetectionOnly'
        line: 'SecRuleEngine On' # 運用環境に合わせてOnに変更
      become: yes
    
    - name: "Copy modsecurity-rules.conf to remote VM"
      ansible.builtin.copy:
        src: "./modsecurity-rules.conf"
        dest: "/etc/nginx/modsec/modsecurity-rules.conf"
        mode: '0644'
      become: yes

    # ------------------------------------------
    # 10. 最終デプロイ検証フェーズ
    # ------------------------------------------
    - name: "Verify all running components"
      ansible.builtin.command:
        cmd: "docker ps"
      become: yes
      register: final_status
      changed_when: false

    - name: "Display running containers"
      ansible.builtin.debug:
        var: final_status.stdout_lines
      
  handlers:
    - name: Restart Nginx
      ansible.builtin.systemd:
        name: nginx
        state: restarted
      become: yes